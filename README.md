A learning project that implements method swizzling to:
  (1) Immitate introducing A/B tests of UI elements in an architecturally clean way 
  (2) Inject functionality into a method belonging to a UIKit class
